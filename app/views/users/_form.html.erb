<div class='well'>
  <%= form_for @user do |f| %>
  <%= render 'shared/errors', obj: @user %>
  <div class='control-group'>
    <%= f.label :Username %>
    <%= f.text_field :username %>
  </div>
  <div class='control-group'>
    <%= f.label :Password %>
    <%= f.password_field :password %>
  </div>
  <%= f.submit @user.new_record? ? "Register" : "Save", class: 'btn btn-success' %>
  <% end %>
</div>
